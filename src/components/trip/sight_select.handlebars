<label for="sight_select"></label>
<select form="trip_form" id="sight_select">
	<option value='-1'>место</option>
	{{#each sights}}
		<option value='{{this.id}}'>{{ this.name }}</option>
	{{/each}}
</select>