export interface UserMetadata {
	id: number; // неплохо для просмотра чужих профилей
	name: string;
	surname: string;
	avatarPath: string;
}

export interface UserEmail {
	email: string;
}

export interface Partisipants {
	id: number;
	profilePhoto: string;
}

export interface tripUser {
	id: number,
}
